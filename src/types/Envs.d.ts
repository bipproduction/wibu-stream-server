export interface Envs {
  PEER_SERVER_NODE_ENV: string;
  DATABASE_URL: string;
  NEXT_PUBLIC_STREAM_DEV_HOST: string;
  NEXT_PUBLIC_STREAM_PRD_HOST: string;
  NEXT_PUBLIC_VAPID_PUBLIC_KEY: string;
  VAPID_PRIVATE_KEY: string;
}